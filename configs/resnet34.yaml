seed: 42
data_dir: "data/NIH_CXR"
output_dir: "outputs/res34/phase2/run1"

img_size: 224
batch_size: 16
num_workers: 2

model_name: "resnet34"
pretrained: true

lr: 0.0003
weight_decay: 0.0001
epochs: 5

# If your dataset is imbalanced, you can enable class weights later.
use_class_weights: true